# gen2: Wi-Fi power save OFF for every connection (overrides default-wifi-powersave-on.conf).
# Power save waits for beacon intervals (~100 ms) and caused 50-200 ms latency tails on robot
# telemetry / camera (desktop measurement 2026-09-26). 2 = disable.
[connection]
wifi.powersave = 2
